See `lambda-function` folder for a typical lambda packaging

The `pygrib` library is unusual in compiling for the AWS lambda environment.  See the pre-written Dockerfile and setup under the `pygrib` folder.